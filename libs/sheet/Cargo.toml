[package]
name = "sheet"
version = "0.1.0"
edition = "2021"

[dependencies]
css = { path = "../css" }
serde = { version = "1.0.217", features = ["derive"] }
regex = "1.11.1"
once_cell = "1.20.2"

[dev-dependencies]
insta = "1.42.1"
serde_json = "1.0.138"
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "my_benchmark"
harness = false
