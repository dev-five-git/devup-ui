export const metadata = {
  title: 'Input',
  alternates: {
    canonical: '/docs/api/input'
  }

}

# Input

The `input` component is a layout primitive that can be used to read user input.

It is just a `input` with some styles.

## How to use

```tsx
const before = <Input border="3px solid black" />

const after = <input className="d0" />
```
